"""HTTP routes this service owns. This service currently has no rows in
gateway/route_registry.yaml (extraction is a pure queue consumer/producer);
add one here + a registry row together if that changes."""
